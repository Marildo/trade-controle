const findAll = (state) => state.all
const current = (state) => state.current
const sum = (state) => state.sum
const lancamentos = (state) => state.lancamentos
const historicoMensal = (state) => state.historicoMensal

export {
  findAll,
  current,
  sum,
  lancamentos,
  historicoMensal
}
